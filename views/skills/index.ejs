<%- include('../partials/header') %>
<a href="/">Go Home</a>
  <ul>
    <% skills.forEach(function(t) { %>
      <li>
        <a href="/skills/<%= t.id %>"><%= t.skill %></a>
          - 
        <%= t.Indorsed ? 'Indorsed' : 'not Indorsed' %>
      </li>
    <% }); %>
  </ul>

<%- include('../partials/footer') %>